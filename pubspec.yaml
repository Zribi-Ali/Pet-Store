name: pettore
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_serializable:

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  http:
  json_annotation:
  font_awesome_flutter:
  

  flutter_lints: ^2.0.0
  build_runner:

flutter:
  uses-material-design: true
